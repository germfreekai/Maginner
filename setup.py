from setuptools import setup

setup(name='maginner',
      version='0.1',
      description='Create banners easily',
      author='germfreekai', 
      packages=['maginner'],
      zip_safe=False)